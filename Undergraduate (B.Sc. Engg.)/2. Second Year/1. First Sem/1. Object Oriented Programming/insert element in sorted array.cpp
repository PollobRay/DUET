//
// Created by pollo on 4/7/2021.
//

#include<stdio.h>
/*
void inserData(int arr[],int n,int d)
{
    int i,find,temp1,temp2;
    find=0;
    for(i=0;i<n;i++)
    {

        if(d<arr[i])
        {
            if(find==0)
            {
                temp1=arr[i];
                arr[i]=d;
            }

            temp2=arr[i+1];
            arr[i+1]=temp1;
            temp1=temp2;
            find++;

        }

    }
    if(find==0)
        arr[n]=d;

}

int main()
{
    int arr[100],n=0,i,d,len;

    scanf("%d",&len);

    for(int k=0;k<len;k++)
    {
        scanf("%d",&d);
        inserData(arr,n++,d);
    }


     for(i=0;i<len;i++)
        printf("%d ",arr[i]);

    return 0;
}
*/