//
// Created by Ray on 8/15/2021.
//

#include <stdio.h>

/*
int main()
{
    int arr[100],singleValue[100],n,p=0,find=0;

    scanf("%d",&n);

    for (int i = 0; i < n; ++i)
    {
        scanf("%d",&arr[i]);

        for (int j = 0; j < p; ++j)
        {
            if (singleValue[j]==arr[i])
            {
                find=1;
                break;
            }
        }

        if (find==0)
        {
            singleValue[p]=arr[i];
            p++;
        }
        find=0;
    }

    printf("\nPrinting data.....\n\n");

    for (int i = 0; i < p; ++i)
    {
        int accurance=0;

        for (int j = 0; j < n; ++j)
        {
            if (singleValue[i]==arr[j])
                accurance++;
        }

        printf("Value: %d \t accurance: %d\n",singleValue[i],accurance);
    }

    return 0;
}
 */