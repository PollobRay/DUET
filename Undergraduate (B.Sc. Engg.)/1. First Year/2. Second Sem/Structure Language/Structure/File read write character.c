//
// Created by Ray on 8/15/2021.
//

#include<stdio.h>

/*
int main()
{
    FILE *fp;
    char ch;

    fp=fopen("file1.txt","w");
    fputs("This is c programming cource",fp);
    fseek(fp,8,SEEK_SET);
    fputs("C programming language",fp);
    fclose(fp);

    fp=fopen("file1.txt","r");
    while ((ch=fgetc(fp))!=EOF)
    {
       printf("%c",ch);
    }

    fclose(fp);

    return 0;
}
*/