//
// Created by Ray on 8/15/2021.
//

#include <stdio.h>
/*
int main()
{
    int arr[100],n,sum,p1,p2,find=0;

    scanf("%d",&n);

    for (int i = 0; i < n; ++i)
    {
        scanf("%d",&arr[i]);
    }

    printf("Sum: ");
    scanf("%d",&sum);

    for (int i = 0; i <n ; ++i)
    {
        p1=i;

        for (int j = p1+1; j <n ; ++j)
        {
            if (arr[p1]+arr[j]==sum)
            {
                find=1;
                p2=j;
                break;
            }
        }

        if (find==1)
        {
            printf("%d   %d\n",arr[p1],arr[p2]);
            break;
        }
    }

    return 0;
}
 */