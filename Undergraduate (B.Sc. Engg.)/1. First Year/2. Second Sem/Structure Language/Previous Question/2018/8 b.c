//
// Created by Ray on 8/16/2021.
//
//dynamic array
#include <stdio.h>
#include <stdlib.h>

/*
int main()
{
    int n,avg;
    int *p;

    scanf("%d",&n);
    //p=(int*)malloc(n*sizeof (int));
    p=(int*)calloc(n,sizeof(int)); // same as above

    for (int i = 0; i < n; ++i)
    {
        scanf("%d",(p+i));
    }

    for (int i = 0; i < n; ++i)
    {
        printf("%d ",*(p+i));
    }

    return 0;
}
 */