//
// Created by Ray on 8/16/2021.
//
//delete 5'th character in a text file

#include<stdio.h>
/*
int main()
{

    char ch;
    FILE *file;


    if ((file=fopen("Text1.txt","r+"))!=NULL)
    {

        while ((ch=fgetc(file))!=EOF)
        {
            if (ftell(file)%5==0)
            {
                fseek(file,-1,SEEK_CUR);
                fputc('-',file);
                fseek(file,1,SEEK_CUR);
            }
        }

        fclose(file);
    }

    return 0;
}
 */