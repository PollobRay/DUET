//
// Created by Ray on 8/16/2021.
//

#include<stdio.h>
/*
int main()
{

    char ch;
    FILE *file;

    // taking input
    if((file=fopen("Text2.txt","w"))!=NULL)
    {
        while ((ch=getchar())!='X')
        {
            fputc(ch,file);
        }

        fclose(file);

    }

    //deleting space
    if ((file=fopen("Text2.txt","r+"))!=NULL)
    {

        while ((ch=fgetc(file))!=EOF)
        {
            if (ch==' ')
            {
                fseek(file,-1,SEEK_CUR);
                fputc('-',file);
                fseek(file,1,SEEK_CUR);
            }
        }

        fclose(file);
    }

    //read file character
    if((file=fopen("Text2.txt","r"))!=NULL)
    {
        while ((ch=getc(file))!=EOF)
        {
            putchar(ch);
        }

        fclose(file);

    }

    return 0;
}
 */